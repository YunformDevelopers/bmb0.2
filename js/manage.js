// JavaScript Document
function ChangeView() {
	var currentView = "list";
	if (currentView == "list" && this.hasClass("list-view") ){
		alert("fsdfs");
		$("#answer-field table.matrix-table").hide();
	
	}
	
}